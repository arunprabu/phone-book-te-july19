import { ColorizrDirective } from './colorizr.directive';

describe('ColorizrDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizrDirective();
    expect(directive).toBeTruthy();
  });
});
